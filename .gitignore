# =================================================================
# AI 서버 프로젝트 .gitignore
# 새로 clone 받은 팀원이 오류 없이 실행할 수 있도록 정리
# =================================================================

# 1. 파이썬 관련 자동 생성 파일들
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 2. 가상환경 (각자 로컬에서 생성)
venv/
env/
ENV/
recipe-ai/
.venv/

# 3. 환경변수 파일 (민감한 정보 포함)
.env
.env.local
.env.production

# 4. IDE 및 에디터 설정 파일
.vscode/
.idea/
*.swp
*.swo
*~
.sublime-project
.sublime-workspace

# 5. 로그 및 임시 파일
*.log
logs/
*.tmp
*.temp
.DS_Store
Thumbs.db
*.pid

# 6. 테스트 및 커버리지 리포트
.coverage
.pytest_cache/
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# 7. Jupyter Notebook
.ipynb_checkpoints

# 8. 인증서 및 보안 관련 파일 (민감한 정보)
certificates/
*.pem
*.key
*.crt
*.p12
*.pfx

# =================================================================
# 9. 사용하지 않는 중복 파일들 제거
# =================================================================

# 메인 파일 중복본들 (main.py만 유지)
app/main_*.py

# API 파일 중복본들 (현재 사용 중인 파일들만 유지)
app/api/search_*.py
!app/api/search.py

# 서비스 파일 중복본들 (script.py만 유지)
app/services/enhanced_search_service_*.py
!app/services/enhanced_search_service_script.py

# 유틸리티 중복본들 (메인 파일만 유지)
app/utils/openai_relevance_scorer_*.py
!app/utils/openai_relevance_scorer.py

# 사용하지 않는 유틸리티 파일
app/utils/smart_score_calculator.py

# 테스트 파일들 (핵심 연결 테스트만 유지)
test_*.py
!test_connection.py

# 배치 파일들 (필요시 각자 로컬에서 생성)
run_*.bat
setup_project.py

# 문서 파일들 (README.md와 API.md만 유지)
*.md
!README.md
!docs/API.md

# 셋업 스크립트들 (setup.py만 유지)
setup.bat
setup.sh

# 기타 임시/백업 파일들
*.bak
*.orig
*.rej
*~
.#*
.*.swp
.*.swo
